---
layout: null
sitemap: false
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
{%- assign pages = site.pages | where_exp: "p", "p.sitemap != false" %}
{%- for p in pages %}
{%- if p.url contains '.xml' or p.url contains '.txt' or p.url contains '404' %}{% continue %}{% endif %}
{%- if p.title %}
  <url><loc>{{ site.url }}{{ p.url }}</loc></url>
{%- endif %}
{%- endfor %}
{%- for post in site.posts %}
  <url><loc>{{ site.url }}{{ post.url }}</loc><lastmod>{{ post.date | date: "%Y-%m-%d" }}</lastmod></url>
{%- endfor %}
{%- for t in site.glossary %}
  <url><loc>{{ site.url }}{{ t.url }}</loc></url>
{%- endfor %}
</urlset>
